requests>=2.31,<3
dateparser>=1.2,<2
icalendar>=5.0,<6
python-dotenv>=1.0,<2
tzdata>=2024.1  # IANA timezone database — zoneinfo needs this explicitly on Windows/some minimal Linux images
python-telegram-bot[job-queue]>=21.0,<22  # needed for bot/ (paid-hosting polling bot) and scripts/ (free GH Actions runners) — plain main.py alone doesn't need it
flask>=3.0,<4  # only needed for webhook/ (the free, no-credit-card alternative to bot/ — see README)
